# ProjectTypeEnum

* `event` - event
* `portrait` - portrait
* `party` - party

## Example Usage

```python
from fstop.models import ProjectTypeEnum

# Open enum: unrecognized values are captured as UnrecognizedStr
value: ProjectTypeEnum = "event"
```


## Values

This is an open enum. Unrecognized values will not fail type checks.

- `"event"`
- `"portrait"`
- `"party"`
